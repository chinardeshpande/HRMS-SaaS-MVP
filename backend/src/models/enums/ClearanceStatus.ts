export enum ClearanceStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  CLEARED = 'cleared',
  PENDING_APPROVAL = 'pending_approval',
  REJECTED = 'rejected',
  ESCALATED = 'escalated'
}
