import { ModernPlaceholder } from '../components/common/ModernPlaceholder';
import { BuildingOfficeIcon } from '@heroicons/react/24/outline';

export default function ModernDepartments() {
  return (
    <ModernPlaceholder
      title="Departments"
      description="Manage organizational departments, hierarchies, and reporting structures"
      icon={BuildingOfficeIcon}
    />
  );
}
